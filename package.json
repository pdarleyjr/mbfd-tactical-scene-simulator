{
  "name": "mbfd-tactical-scene-simulator",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.20.0",
  "engines": {
    "node": ">=22.13"
  },
  "scripts": {
    "dev": "pnpm --parallel --filter @mbfd/server --filter @mbfd/web dev",
    "dev:e2e": "pnpm --parallel --filter @mbfd/server --filter @mbfd/web dev:e2e",
    "build": "pnpm -r --if-present build",
    "typecheck": "tsc -b --pretty false",
    "lint": "oxlint apps packages",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:e2e": "playwright test",
    "db:generate": "pnpm --filter @mbfd/server db:generate",
    "db:migrate": "pnpm --filter @mbfd/server db:migrate"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^24.13.2",
    "@vitest/coverage-v8": "^4.1.10",
    "oxlint": "^1.69.0",
    "typescript": "~6.0.2",
    "vitest": "^4.1.10"
  }
}
